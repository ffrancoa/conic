[package]
name        = "conic-core"
version     = "0.1.0"
edition     = "2024"
license     = "MPL-2.0"
description = "Command-line interface for `conic`"

[dependencies]
polars      = { version = "0.51", features = ["lazy", "is_in"] }
thiserror   = { version = "2.0.16" }

[dev-dependencies]
criterion   = { version = "0.7" }

[[bench]]
name        = "conic_bench"
harness     = false