[package]
name        = "conic-core"
version     = "0.1.0"
edition     = "2024"
license     = "MPL-2.0"
description = "Command-line interface for `conic`"

[dependencies]
polars      = { version = "0.50", features = ["lazy", "is_in"] }

[dev-dependencies]
criterion   = { version = "0.7" }

[[bench]]
name        = "filter_out_rows"
harness     = false